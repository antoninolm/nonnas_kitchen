# MongoDB Atlas connection string
MONGODB_URI=

# Secret used to sign and verify JWTs
JWT_SECRET=

# Stripe TEST MODE secret key (sk_test_...) — never commit a live key
STRIPE_SECRET_KEY=

# Deployed client origin — used for CORS and Stripe checkout redirect URLs
CLIENT_URL=http://localhost:5173
