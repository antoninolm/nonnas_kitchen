# Base URL of the Express API.
# Leave empty in development — the Vite dev server proxies /api requests
# to http://localhost:8080 (see vite.config.js).
# In production (Vercel), set this to the Render API URL, no trailing slash,
# e.g. https://nonnas-kitchen-api.onrender.com
VITE_API_URL=
